/**
 * 用于词法分析的模板类
 */
public class Template {
	public static void main() {
        while(a < b || c < d && e < f) {
            if(c < d) {
                x = y + z;
                x = y;
            }
        }
	}
}
$